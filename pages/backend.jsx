import {ContentPageHeader} from '../components/headers'

function BackendPage() {
    return(
        <>
        <ContentPageHeader type='backend developers' title='the next big thing' tagline='Makes the last big thing useable'/>
        </>
    );
}

export default BackendPage;