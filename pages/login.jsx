import {ContentPageHeader} from '../components/headers'

function LoginPage() {
    return(
        <>
        <ContentPageHeader type='login' title='D20 Developers' tagline='We always roll high initaitive'/>
        </>
    );
}

export default LoginPage;