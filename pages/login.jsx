import {ContentPageHeader} from '../components/headers'

function LoginPage() {
    return(
        <>
        <ContentPageHeader type='login' title='the next big thing' tagline='Makes the last big thing useable'/>
        </>
    );
}

export default LoginPage;